# 开发工程师 — Amelia Agent Seed Prompt
> 备选方案：如果 BMAD 已安装，直接在 AI IDE 中输入 `/dev` → 选择 `DS` 即可。

## Prompt
```
DS

Implement a new feature in the existing Calc24 codebase.

Tech: Vue 3 (Composition API) + TypeScript + Vite + SCSS
Existing patterns to follow:
- Composables in src/composables/ (useXxx pattern)
- Components in src/components/ (single-file .vue)
- Utils in src/utils/

Requirements:
- Follow existing code style and conventions
- Use existing composables where applicable
- Ensure TypeScript types are complete
- Must pass vue-tsc type checking

Before writing code:
1. Read existing patterns in relevant files
2. Follow the same coding style
3. Use existing utilities/helpers where available
```
